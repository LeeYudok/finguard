# AGENTS.md — testdata/rule-fixtures/

픽스처 규약(`EXPECT:` 마커 3형태, `safe_` 대조군, 하위 디렉터리 순회, 시크릿 형태 픽스처의
GitHub 시크릿 스캐닝 제외, 합성값 원칙)은 **[`rules/AGENTS.md`](../../rules/AGENTS.md)** 를 따른다.
여기에 `.yml`/`.yaml` 픽스처를 두는 것은 허용되지만 `rules/` 아래로 옮기면 룰셋 전체가 깨진다 (#43).
